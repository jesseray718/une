# Agape–Prime Scaling Law (locked)

Tier branching factor   ≈ 7          (nearest practical integer to φ⁴ ≈ 6.854)
Reward multiplier       = φ^min(t, 50) × Υ

where
  φ   = (1 + √5)/2
  t   = verified epochs of continuous positive yield
  Υ   = verified physical yield factor (0 ≤ Υ ≤ 1)
        Υ = 0 when no measurable physical output exists

Constraint
  Higher-tier influence may increase only when Υ > 0 
  for the agents under its care.
  This is the operational expression of Agape.
